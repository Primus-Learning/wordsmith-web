apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wordsmith-web.fullname" . }}-test-connection"
  labels:
    {{- include "wordsmith-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wordsmith-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
